{
    "scripts": {
        "options": {
            "pattern": "css\\(|show\\(|hide\\("
        },
        "src": ["<%= symfony.src %>/**/scripts/*.js"]
    },
    "templates": {
        "options": {
            "pattern": "style=|<style"
        },
        "src": ["<%= symfony.src %>/**/*.html.twig"]
    }
}
