{
    "develop": {
        "dir": "<%= app.source %>"
    },
    "options": {
        "rulesets": "<%= app.rules %>/naming.xml,<%= app.rules %>/codesize.xml,<%= app.rules %>/unusedcode.xml",
        "reportFormat": "text",
        "strict": "true",
        "exclude": "temp"
    }
}
