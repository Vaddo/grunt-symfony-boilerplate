{
    "develop": {
        "dir": "<%= symfony.src %>"
    },
    "options": {
        "rulesets": "<%= app.tests %>/naming.xml,<%= app.tests %>/codesize.xml,<%= app.tests %>/unusedcode.xml",
        "reportFormat": "text",
        "strict": "true",
        "exclude": ""
    }
}
